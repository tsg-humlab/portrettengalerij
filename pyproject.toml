[project]
name = "portrettengalerij"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.1",
]

[dependency-groups]
prod = [
    "gunicorn>=24.1.1",
]
